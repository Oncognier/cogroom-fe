'use client';

import styled from '@emotion/styled';

const EmailForm = styled.div`
  display: flex;
  align-items: center;
  gap: ${({ theme }) => theme.spacing[16]};

  width: 100%;
`;

const S = {
  EmailForm,
};

export default S;
